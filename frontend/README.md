# Frontend

This folder contains the React + Vite + TailwindCSS UI for AI Incident Assistant.

## Run locally

```bash
npm install
npm run dev
```

The frontend sends requests to:

```text
POST http://127.0.0.1:8000/ask
```
